import type { NextPage } from "next";
import ModelCard from "@components/molecules/ModelCard/ModelCard";

const Home: NextPage = () => {
  return (
    <div>
      <ModelCard />
    </div>
  );
};

export default Home;
